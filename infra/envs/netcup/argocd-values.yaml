# infra/envs/netcup/argocd-values.yaml
# NOTA: no definir extraObjects aquí — vive completo (ConfigMap del plugin +
# HTTPRoute de la UI + Ingress gRPC de argo-api) en
# infra/apps/argocd/values-helmfile.yaml.gotmpl, porque Helm reemplaza las
# listas enteras al fusionar values.
{}
