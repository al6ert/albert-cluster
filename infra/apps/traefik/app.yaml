# Descubierto por el ApplicationSet (git files generator).
# Crear este fichero = la app aparece en ArgoCD; borrarlo = se poda.
name: traefik
namespace: traefik   # destination.namespace de la Application
wave: "1"       # documental: orden de bootstrap / helmfile raíz
