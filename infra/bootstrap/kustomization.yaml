apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # Namespaces y CRDs (fase 0)
  - namespaces/
  - crds/
  - rbac/gh-actions.yaml
  # SealedSecrets y Middlewares (fase 1)
  - secrets/
  - middlewares/
  # ArgoCD Applications (fase 2)
  - argocd-root.yaml
  - argocd-minikube.yaml
