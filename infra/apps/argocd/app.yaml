# Descubierto por el ApplicationSet (git files generator).
# Crear este fichero = la app aparece en ArgoCD; borrarlo = se poda.
name: argocd
namespace: argocd   # destination.namespace de la Application
wave: "2"       # documental: orden de bootstrap / helmfile raíz
