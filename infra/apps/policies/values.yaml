# Políticas día-2 por namespace de APP (no de plataforma): NetworkPolicy,
# ResourceQuota y LimitRange. Viven aquí (reconciliadas por ArgoCD) y no en
# bootstrap porque cambian con las apps.
#
# ⚠️ NetworkPolicy en este cluster (verificado 2026-07):
#   - netcup usa flannel, que NO aplica NetworkPolicy → declarativas hasta
#     migrar el CNI (Cilium/Calico). Se declaran igualmente: documentan la
#     intención y se activan solas al cambiar el CNI.
#   - minikube (CNI por defecto) tampoco las aplica.
# Solo ingress: default-deny de egress es alta fricción para operar en
# solitario (DNS, pulls, webhooks); se documenta como paso futuro.
#
# Primera iteración deliberadamente SOLO en namespaces de apps (hello, admin).
# NO añadir default-deny en cert-manager/argocd/traefik sin mapear antes sus
# flujos (webhooks del apiserver, gRPC interno, etc.).

resources:
  # ---------------- hello ----------------
  - apiVersion: networking.k8s.io/v1
    kind: NetworkPolicy
    metadata:
      name: default-deny-ingress
      namespace: hello
    spec:
      podSelector: {}
      policyTypes: [Ingress]
  - apiVersion: networking.k8s.io/v1
    kind: NetworkPolicy
    metadata:
      name: allow-from-traefik
      namespace: hello
    spec:
      podSelector: {}
      policyTypes: [Ingress]
      ingress:
        - from:
            - namespaceSelector:
                matchLabels:
                  kubernetes.io/metadata.name: traefik
  - apiVersion: networking.k8s.io/v1
    kind: NetworkPolicy
    metadata:
      name: allow-from-monitoring
      namespace: hello
    spec:
      podSelector: {}
      policyTypes: [Ingress]
      ingress:
        - from:
            - namespaceSelector:
                matchLabels:
                  kubernetes.io/metadata.name: monitoring
  - apiVersion: networking.k8s.io/v1
    kind: NetworkPolicy
    metadata:
      name: allow-same-namespace
      namespace: hello
    spec:
      podSelector: {}
      policyTypes: [Ingress]
      ingress:
        - from:
            - podSelector: {}
  - apiVersion: v1
    kind: ResourceQuota
    metadata:
      name: quota
      namespace: hello
    spec:
      hard:
        requests.cpu: 500m
        requests.memory: 256Mi
        limits.cpu: "1"
        limits.memory: 512Mi
        pods: "10"
  - apiVersion: v1
    kind: LimitRange
    metadata:
      name: defaults
      namespace: hello
    spec:
      limits:
        - type: Container
          # Para que pods de charts de terceros sin resources hereden límites
          # (sin esto, con ResourceQuota activa el pod se RECHAZA)
          default:
            cpu: 200m
            memory: 256Mi
          defaultRequest:
            cpu: 50m
            memory: 64Mi

  # ---------------- langfuse ----------------
  # Stack completo (web+worker+postgres+clickhouse+valkey+minio): la quota
  # tiene que dar cabida a los resources de infra/apps/langfuse/values.yaml
  # (~1,2 CPU / ~2,7Gi requests; ~5,3 CPU / ~7,3Gi limits) + jobs de migración.
  - apiVersion: networking.k8s.io/v1
    kind: NetworkPolicy
    metadata:
      name: default-deny-ingress
      namespace: langfuse
    spec:
      podSelector: {}
      policyTypes: [Ingress]
  - apiVersion: networking.k8s.io/v1
    kind: NetworkPolicy
    metadata:
      name: allow-from-traefik
      namespace: langfuse
    spec:
      # Solo la web recibe tráfico del Gateway; el resto del stack es interno
      podSelector: {}
      policyTypes: [Ingress]
      ingress:
        - from:
            - namespaceSelector:
                matchLabels:
                  kubernetes.io/metadata.name: traefik
  - apiVersion: networking.k8s.io/v1
    kind: NetworkPolicy
    metadata:
      name: allow-from-monitoring
      namespace: langfuse
    spec:
      podSelector: {}
      policyTypes: [Ingress]
      ingress:
        - from:
            - namespaceSelector:
                matchLabels:
                  kubernetes.io/metadata.name: monitoring
  - apiVersion: networking.k8s.io/v1
    kind: NetworkPolicy
    metadata:
      name: allow-same-namespace
      namespace: langfuse
    spec:
      # web/worker ↔ postgres/clickhouse/valkey/minio
      podSelector: {}
      policyTypes: [Ingress]
      ingress:
        - from:
            - podSelector: {}
  - apiVersion: v1
    kind: ResourceQuota
    metadata:
      name: quota
      namespace: langfuse
    spec:
      hard:
        requests.cpu: "2"
        requests.memory: 4Gi
        limits.cpu: "6"
        limits.memory: 9Gi
        pods: "20"
  - apiVersion: v1
    kind: LimitRange
    metadata:
      name: defaults
      namespace: langfuse
    spec:
      limits:
        - type: Container
          # Jobs de migración y helpers de los subcharts sin resources propios
          default:
            cpu: 500m
            memory: 512Mi
          defaultRequest:
            cpu: 50m
            memory: 128Mi
