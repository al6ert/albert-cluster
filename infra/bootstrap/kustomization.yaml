apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Namespaces primero (wave 0)
  - namespaces/
  # CRDs y RBAC
  - crds/
  - rbac/gh-actions.yaml
  # SealedSecrets (después de namespaces)
  - secrets/
  # Middlewares comunes
  - middlewares/
  # ArgoCD Applications
  - argocd.yaml
  - argocd-root.yaml
  - argocd-minikube.yaml
  # Nota: Las ArgoCD Applications ahora se manejan por separado en infra/apps/
